/** Background off-exchange limit refresh interval while the comparison view is open. */
export const LIMITS_REFRESH_INTERVAL_MS = 30 * 60 * 1000;

/** Delay before the first background limit refresh after the comparison view loads. */
export const LIMITS_INITIAL_DELAY_MS = 60 * 1000;
