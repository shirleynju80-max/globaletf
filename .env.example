# Local development (UI talks to localhost API)
VITE_API_BASE=http://127.0.0.1:8787

# API server (local)
PORT=8787
HOST=127.0.0.1
DATABASE_PATH=data/etflimit.sqlite

# Monolith only — leave unset for option B API deploy
# SERVE_STATIC=1
# STATIC_ROOT=dist
